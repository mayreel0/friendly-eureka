# Android Surfaces

Android guest AR starts as a Chrome WebXR fast-follow path.

The MVP does not include a native installed Android guest app. Android merchant recording is deferred until the first iOS recorder and guest replay loop work on one real route.
