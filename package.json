{
  "name": "lechigo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:pilot": "node scripts/dev-pilot-flow.mjs",
    "test": "node --test \"packages/**/*.test.ts\" \"apps/**/*.test.ts\"",
    "typecheck": "tsc --project tsconfig.base.json",
    "lint": "npm run typecheck",
    "format": "node scripts/check-format.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.0",
    "@types/qrcode": "^1.5.6",
    "jsqr": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
